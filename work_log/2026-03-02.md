# 2026-03-02

## 작업 내용

### 1. 게시판 (Community Thread) 페이지 추가

더미 데이터 기반 UI를 구성했다. AWS 연결 없이 커뮤니티 피드/상세 화면을 완성했다.

#### 구조 변경: React Router 도입

`react-router-dom v6`을 추가하고, 기존 단일 App.tsx를 라우팅 구조로 분리했다.

**라우트 설계:**
- `/` → `EditorPage` (기존 에디터)
- `/thread` → `ThreadBoard` (커뮤니티 게시판)
- 두 라우트 모두 `RootLayout` 아래에서 Sidebar 공유

#### 파일 구조

```
src/
├── main.tsx              ← RouterProvider로 교체
├── router.tsx            ← createBrowserRouter 정의 (신규)
├── layouts/
│   └── RootLayout.tsx    ← Sidebar + Outlet + JWT 초기화 (신규)
└── pages/
    ├── EditorPage.tsx    ← App.tsx 에디터 내용 추출 (신규)
    └── ThreadBoard/
        ├── index.tsx     ← 중간+우측 패널 레이아웃 (신규)
        ├── ThreadFeed.tsx   ← 게시글 피드 + 탭 + 검색 (신규)
        ├── PostCard.tsx  ← 게시글 카드 컴포넌트 (신규)
        ├── ThreadDetail.tsx ← 상세 + 댓글 (신규)
        └── dummyData.ts  ← 더미 데이터 (신규)
```

#### 책임 재배분

| 이전 (App.tsx) | 이후 |
|---|---|
| Sidebar 렌더링 | RootLayout.tsx |
| JWT 추출 + loadUserInfo + loadWorks | RootLayout.tsx |
| 에디터 상태 (viewMode, plotMode 등) | EditorPage.tsx |
| beforeunload + Cmd+S 핸들러 | EditorPage.tsx |

기존 `App.tsx`는 삭제하지 않고 그대로 둠 (더 이상 import되지 않는 dead code).

#### ThreadBoard UI

**중간 패널 (ThreadFeed):**
- 탭: 광장 | 내 글 | 알림
- 검색창 + "새로운 글 쓰기" 버튼
- PostCard 목록

**PostCard:**
- 작가 아바타 (이니셜 원형, 고유 색상)
- 제목 + 설명
- 씬 미리보기 (씬 헤딩 + 장소/시간 + 대사 2개)
- 태그 배지
- 조회/좋아요/댓글 수 + "자세히 보기" 버튼

**우측 패널 (ThreadDetail):**
- 게시글 전체 미리보기
- 최신순/인기순 정렬
- 댓글 목록 (댓글 수에 따라 동적 렌더링)
- 댓글 입력 + 등록 (로컬 상태로 더미 추가)
- 하단: 전체 게시글 수 + 온라인 작가 수

#### 더미 데이터

`dummyData.ts`에 6개 게시글, 14개 댓글. 한국 문학/희곡 소재.
각 작가마다 고유 avatar 색상 지정.

#### Community 버튼

EditorPage의 우측 패널 탭 바 우측에 `<Link to="/thread">🌐 커뮤니티</Link>` 추가.
커뮤니티 → 에디터 복귀는 브라우저 뒤로가기 또는 `/` URL로 이동.

#### CSS 추가 (App.css)

- `.post-card`, `.post-card--selected` — 카드 테두리, hover/selected 상태
- `.post-avatar`, `.post-avatar--sm` — 원형 아바타
- `.post-tag` — 태그 배지 (연한 회색 배경 + 진한 텍스트)
- `.scene-preview`, `.scene-preview__heading`, `.scene-preview__meta`, `.scene-preview__dialogues` — 씬 미리보기 블록
- `.dialogue-preview`, `.dialogue-preview__name`, `.dialogue-preview__text` — 대사 라인
- `.thread-tab`, `.thread-tab--active` — 게시판 탭

#### 패키지 추가

- `react-router-dom` — `npm install react-router-dom`

## 검증

- `npm run build` — ✅ 0 TypeScript 오류, 빌드 성공
- 라우팅: `/` EditorPage, `/thread` ThreadBoard, 공유 Sidebar

## 다음 작업

1. 게시글 공유 기능 (내 작품 → 게시판 공유 모달)
2. 댓글 AWS 백엔드 연결
3. ThreadBoard Community 버튼에서 돌아오는 링크 추가 (현재 브라우저 뒤로가기만)
4. 내 글 탭 — 현재 로그인 유저 기준 필터 연결
